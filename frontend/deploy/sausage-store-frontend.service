[Unit]
Description=http-server-my
After=network.target
Requires=network.target

[Service]
Type=simple
Restart=always
RestartSec=2
User=root

Environment=LOG_PATH=/var/log/

ExecStart=http-server /var/www-data/frontend/ -p 80 --proxy http://localhost:8080

[Install]
WantedBy=multi-user.target
